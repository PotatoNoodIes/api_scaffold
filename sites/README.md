# sites/

Static explainer page for **api-scaffold** itself — purpose, quickstart, command
reference, generated output, and architecture.

Self-contained single-file HTML (fonts embedded as base64, no external
requests, no build step). Open `index.html` directly in a browser, or serve
the folder with any static file server:

```sh
cd sites
python3 -m http.server 8000
```
