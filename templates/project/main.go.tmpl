package main

import (
	"log"

	"{{.ModulePath}}/internal/config"
	"{{.ModulePath}}/internal/router"
)

func main() {
	cfg := config.Load()
	r := router.New(cfg)

	log.Printf("{{.ProjectName}} listening on :%s", cfg.Port)
	if err := r.Run(":" + cfg.Port); err != nil {
		log.Fatal(err)
	}
}
